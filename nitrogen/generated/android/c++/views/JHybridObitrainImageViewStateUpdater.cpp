///
/// JHybridObitrainImageViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridObitrainImageViewStateUpdater.hpp"
#include "views/HybridObitrainImageViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::obitrain_reactnativeimage::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridObitrainImageViewState>;

void JHybridObitrainImageViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridObitrainImageViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  std::shared_ptr<JHybridObitrainImageViewSpec> hybridView = javaView->getJHybridObitrainImageViewSpec();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridObitrainImageViewState& data = concreteState->getData();
  const std::shared_ptr<HybridObitrainImageViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    // Props aren't set yet!
    throw std::runtime_error("HybridObitrainImageViewState's data doesn't contain any props!");
  }

  // Update all props if they are dirty
  if (props->uri.isDirty) {
    hybridView->setUri(props->uri.value);
    props->uri.isDirty = false;
  }
  if (props->resource.isDirty) {
    hybridView->setResource(props->resource.value);
    props->resource.isDirty = false;
  }
  if (props->resizeMode.isDirty) {
    hybridView->setResizeMode(props->resizeMode.value);
    props->resizeMode.isDirty = false;
  }
  if (props->tintColor.isDirty) {
    hybridView->setTintColor(props->tintColor.value);
    props->tintColor.isDirty = false;
  }
  if (props->decodeWidth.isDirty) {
    hybridView->setDecodeWidth(props->decodeWidth.value);
    props->decodeWidth.isDirty = false;
  }
  if (props->decodeHeight.isDirty) {
    hybridView->setDecodeHeight(props->decodeHeight.value);
    props->decodeHeight.isDirty = false;
  }
  if (props->recyclingKey.isDirty) {
    hybridView->setRecyclingKey(props->recyclingKey.value);
    props->recyclingKey.isDirty = false;
  }
  if (props->onError.isDirty) {
    hybridView->setOnError(props->onError.value);
    props->onError.isDirty = false;
  }

  // Update hybridRef if it changed
  if (props->hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props->hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
    props->hybridRef.isDirty = false;
  }
}

} // namespace margelo::nitro::obitrain_reactnativeimage::views
